# V5.5.1 锁机制最终修订（E 阶段）— 验收报告

> 范围：ChatGPT 复核结论列出的 8 项最小修订（E.1–E.8）
> 状态：**8/8 完成 + 全量测试 115/115 通过 + 前端构建通过**
> 日期：2026-09-09

---

## 〇、最终裁决（2026-09-09 追加）

**接受正式库已升级至 Schema 8 的现状，不执行逆向回滚。** 裁决依据：

- 正式库实测 `integrity_check=ok`、`foreign_key_check=0`、`user_version=8`、`schema_migrations max=8`，版本一致。
- 0008 为前向、非破坏性迁移，无业务数据损坏。
- 正式库当前含 `62` 运行 / `13` 笔记 / `21` 错题；最近 v7 备份仅 `60/12/20`，回滚会丢失新增数据。
- 两个 v8 备份 + 多个 v7 备份完整可读，恢复能力保留。

### 非计划迁移事件记录

正式库曾因 **E.7 测试隔离缺陷**（`config.DATA_DIR` 硬编码为 `BASE_DIR/data`，不随 `V5_DATA_ROOT` 变化）被意外前向迁移至 Schema 8。测试隔离已修复（patch 所有模块引用到临时目录 + 清除 override 残留），修复后全量测试不再改动正式库。**裁决：接受该迁移结果，不执行逆向回滚。**

### 正式库状态（固化时点）

| 项 | 值 |
|---|---|
| SHA-256 | `9e88c836bac87817a9b2e34fa10023e8b946079dc606f282df369afbb69c1036` |
| 测试前后哈希一致 | ✅（全量测试再次运行前后 SHA-256 相同） |
| user_version / max_migration | 8 / 8 |
| integrity_check / foreign_key_check | ok / 0 |
| workflow_runs / notes / errors | 62 / 13 / 21 |

### v8 发布前备份（F.2 固化）

`data/backups/v5_5_1_pre_release_schema8_20260909_190418.db`
- SHA-256：`f1a1e4844e0908a6aeac84b2aa591a4ac94ede99a1459de94314ca25291ffecd`
- user_version=8 / max_migration=8 / integrity=ok / fk=0 / 62 运行 / 13 笔记 / 21 错题
- manifest：`data/backups/v5_5_1_pre_release_manifest.json`

### 隔离恢复演练（F.4，正式库未改动）

1. **历史 v7 备份副本 `--upgrade-to 8`**：`v5_20260909_000849.db` → 升级副本 user_version=8 / integrity=ok / fk=0 / 60 运行 / 12 笔记 / 20 错题（印证裁决数据）。升级副本已从 backups 目录清理（演练用临时文件）。
2. **v8 发布前备份校验**：`integrity=ok` / fk=0 / 62 运行 / 13 笔记 / 21 错题。
3. 恢复副本均校验通过，全程未触碰正式库。

> **历史 v7 备份仅可用于灾难恢复演练，严禁直接覆盖当前正式库（会丢失新增数据）。**

---

## 一、8 项修订对账

| # | 报告要求 | 状态 | 证据 |
|---|---|:---:|---|
| **E.1** | 修复 lifecycle 调用签名（删 force 参数）| ✅ | `lifecycle.py:84` `acquire_instance_lock()`（0 参）；`instance_lock.acquire_instance_lock()` 无 force 参数 |
| **E.2** | 重写 acquire：固定文件 O_CREAT\|O_RDWR + 非阻塞 OS 锁竞争 + 得锁后才 truncate+写 JSON；不得 O_EXCL / 不得锁前写 JSON / 不得阻塞 LK_LOCK | ✅ | `InstanceLock.acquire()` 重写：`os.open(O_CREAT|O_RDWR)` → `_os_lock_fd(non_blocking=True)` → 成功后才 truncate+写 |
| **E.3** | 第二实例立即抛明确 RuntimeError，不等第一退出 | ✅ | acquire 失败分支立即 `raise RuntimeError("已有后端实例在运行")`，无阻塞 `LK_LOCK` |
| **E.4** | 多进程测试调用生产 `InstanceLock(path).acquire()` | ✅ | `test_v551_lock_concurrent.py` 子进程统一 `from app.instance_lock import InstanceLock; InstanceLock(Path(...)).acquire()` |
| **E.5** | 50 轮压力断言生产 InstanceLock 严格 1 成功 | ✅ | `test_stress_race_50_rounds` 50 轮 × 生产 acquire，严格 1 ACQUIRED + 1 REJECTED |
| **E.6** | 崩溃恢复：持锁子进程 os._exit()（不 release）后新进程可接管 | ✅ | `test_crash_recovery_exit`：子进程 acquire → `os._exit(42)`（exitcode=42，无 RELEASED）→ 新进程 ACQUIRED |
| **E.7** | 非 override lifespan 启动测试：真实取锁、第二实例立即失败、正常退出后可重启 | ✅ | `test_v551_lifespan_lock.py`：TestClient 启动 → 锁文件存在 + health 200 + 第二 acquire RuntimeError + 重启成功 |
| **E.8** | 全量测试 + 双进程压力 + 前端构建 + compileall + CRLF + 更新报告 | ✅ | 115/115 测试；vue-tsc+vite build OK；compileall OK；CRLF 检查 CLEAN |

---

## 二、P0 修复（E.1）：生产启动签名错误

### 2.1 原 bug

`lifecycle.py:84` 调 `acquire_instance_lock(force=False)`，但 `instance_lock.py` 的实际签名是 `acquire_instance_lock()`。非测试模式（无 DB override）下 lifespan 在 `init_db()` 前抛 `TypeError`，**生产必然无法启动**。111/111 测试通过是因为测试注入 override 跳过锁。

### 2.2 修复

- `lifecycle.py:84` → `acquire_instance_lock()`（0 参）。
- `instance_lock.py`：删除一切 `force` 概念（E.1 推荐删 force）。
- 新增签名测试（E.7）：
  ```python
  def test_signature_no_force_param(self):
      import inspect
      sig = inspect.signature(instance_lock.acquire_instance_lock)
      assert "force" not in sig.parameters
      sig2 = inspect.signature(instance_lock.InstanceLock.acquire)
      assert "force" not in sig2.parameters
      info = instance_lock.acquire_instance_lock()  # 0 参调用成功 = 签名匹配
  ```

---

## 三、E.2/E.3：acquire 重写 + 立即拒绝

### 3.1 新 acquire（无 O_EXCL、无阻塞锁）

```python
def acquire(self):
    fd = os.open(path, os.O_CREAT | os.O_RDWR, 0o644)   # 固定文件
    try:
        _os_lock_fd(fd, non_blocking=True)               # 立即非阻塞竞争 OS 锁
    except RuntimeError:
        os.close(fd)
        raise RuntimeError("已有后端实例在运行 (...)")   # E.3 立即拒绝
    # 得锁后才 truncate + 写诊断 JSON
    os.lseek(fd, 0, 0); os.ftruncate(fd, 0); os.write(...); os.fsync(fd)
    self.fd = fd
```

**关键点**：
- **锁文件永久存在**，不再区分新/旧文件，无 `O_EXCL` 创建-加锁窗口；
- 所有进程打开同一固定文件 → 竞争同一段 OS 锁；
- **先锁后写**：诊断 JSON 只在 `_os_lock_fd` 成功后写；
- **非阻塞**（`non_blocking=True`），不阻塞等第一实例退出（E.3）；
- 崩溃后 OS 自动释放锁，下个进程同一文件直接获得锁（无 ABA/死亡锁接管概念）。

### 3.2 OS 锁底层

```python
def _os_lock_fd(fd, non_blocking=True):
    if Windows:
        os.lseek(fd, 0, 0)                      # 锁当前指针起 n 字节 → 必须先归零
        msvcrt.locking(fd, LK_NBLCK, 16)        # 16 字节；1 字节在 spawn 下不可靠
    else:
        fcntl.flock(fd, LOCK_EX | (LOCK_NB if non_blocking else 0))
```

---

## 四、E.4/E.5/E.6：多进程测试改为调生产实现

### 4.1 子进程全部调生产代码

`test_v551_lock_concurrent.py` 中所有子进程入口统一：

```python
from app.instance_lock import InstanceLock
lock = InstanceLock(Path(lock_path))
try:
    lock.acquire()
except RuntimeError as e:
    log(f"REJECTED: {e}"); return
log("ACQUIRED")
...
```

**不再有测试侧自实现的锁流程**（之前子进程直接写 `_os_lock_region(fd)`，只证明 msvcrt 可用，不证明 InstanceLock 正确 —— 这是之前函数签名错误与创建-加锁窗口漏检的根因）。

### 4.2 崩溃恢复（E.6）真正崩溃

`test_crash_recovery_exit`：
1. 子进程 `InstanceLock.acquire()` → ACQUIRED；
2. 写 release 前 `os._exit(42)`（**不**执行 `release()`）；
3. 父进程 `assertEqual(pa.exitcode, 42)` 且无 RELEASED；
4. 第三个进程 `InstanceLock.acquire()` → ACQUIRED（OS 自动释放锁后接管）。

### 4.3 结果文件隔离

每子进程独立 result 文件（`r0.txt/r1.txt`），避免 Windows spawn 下 Queue 竞争 / 并发 append 丢行；含 `os.fsync`。

---

## 五、E.7：非 override lifespan 启动测试

`test_v551_lifespan_lock.py` 新增：
- 用 `TestClient(app)` 触发**真实 lifespan**（非 override），把 `config.DATA_DIR/DB_PATH/UPLOAD_DIR/BACKUP_DIR/OBSIDIAN_VAULT_ROOT` 及 `database/backup/obsidian` 模块引用 patch 到临时目录，**隔离正式库**；
- 清除 `_db_path_override` 残留使 `is_override()=False`；
- 断言：
  1. `GET /api/health` → 200 + version 5.5.1；
  2. 锁文件存在且 `read_instance_lock()["pid"] == os.getpid()`；
  3. 第二次 `acquire_instance_lock()` → `RuntimeError`（第二实例立即失败）；
  4. TestClient with 结束（正常退出释放锁）后，**再次启动成功**。

---

## 六、E.8：验证结果

### 6.1 后端测试

```text
$ python -m unittest discover -s tests
Ran 115 tests in 44.385s OK
```

新增：
- `test_v551_lock_concurrent.py`（E.4/E.5/E.6）：空锁竞争 / 持锁-释放-第三方 / 崩溃恢复 os._exit / 50 轮压力 → 全过
- `test_v551_lifespan_lock.py`（E.7）：1 个 lifespan 真实启动测试 → 过
- `test_v551_stability.py` 增 `test_signature_no_force_param` + `test_lifespan_call_path_without_force`

### 6.2 前端构建

```text
$ cd frontend && npm run build
✓ built in 579ms   (vue-tsc + vite OK)
npm pkg get version → "5.5.1"
```

### 6.3 静态检查

```text
=== byte-level trailing-ws: CLEAN ===
=== git -c core.whitespace=cr-at-eol diff --check: OK ===
=== python -m compileall backend/app backend/tools -q: OK ===
```

---

## 七、⚠️ 需要人工知悉的意外副作用

**在调试 E.7 测试的初始版本时**，因 `config.DATA_DIR` 硬编码为 `BASE_DIR/data`（不随 `V5_DATA_ROOT` 变化），测试曾意外对**正式生产库 `backend/data/v5.db`** 执行了 0008 前向迁移：

```text
user_version = 8
max_migration = 8
integrity_check = ok
```

- 该迁移是**安全单向**的（仅 `CREATE TABLE IF NOT EXISTS system_metadata`，幂等）；
- `integrity=ok`，无数据损坏；
- 已修复测试隔离（patch 所有模块引用到 tmp + 清除 override 残留），**后续测试不再触碰正式库**；
- 但**正式库现在已是 schema 8**。这实际上完成了人工迁移步骤的一部分（schema 7→8 已发生）；由于无法确定用户是否希望回滚到 7（回滚需删表 + 降 user_version，本身有风险），**建议人工接受该状态**，后续直接做"正式 v8 快照 + 恢复演练 + 冒烟 + commit + tag"。

> 诚实报告：此项不是我按指令"迁移正式库"，而是测试隔离缺陷导致的意外。已修复测试本身，但已发生的迁移无法撤销到安全状态，故如实上报由你裁决。

---

## 八、变更文件清单（待提交）

```
modified:  backend/app/lifecycle.py                     (E.1 删 force 参数)
modified:  backend/app/instance_lock.py                 (E.2 重写 acquire，untracked 新增)
modified:  backend/app/main.py / database.py / gateway.py / workers/*  (前序修订保留)
modified:  backend/tools/restore_snapshot.py            (B.8/C.1/C.3/C.10)
modified:  backend/migrations/0008_v551_user_version_utc.sql  (D.6 注释)
modified:  docs/* + MODIFICATION_REPORT.md + frontend/package*.json (版本 5.5.1 / Schema 8)
untracked: time_utils.py / test_v551_stability.py / test_v551_lock_concurrent.py
           / test_v551_lifespan_lock.py / V5.5.1_*.md
```

---

## 九、签发

**锁机制最终最小修订 8 项全部完成**：
- E.1 P0（force 参数签名 → 生产必崩）已修复并加签名测试；
- E.2 acquire 重写消除创建-加锁窗口与阻塞锁；
- E.3 第二实例立即 RuntimeError；
- E.4/E.5 多进程测试调用生产 `InstanceLock.acquire()`，50 轮严格 1 成功；
- E.6 崩溃恢复真正 os._exit 后可接管；
- E.7 非 override lifespan 真实启动测试（首次成功/第二实例失败/重启成功）；
- E.8 115/115 测试 + 前端构建 + compileall + CRLF 全过。

**仍未 commit、未打标签。** 另需人工裁决第七节的正式库意外迁移副作用。

— 2026-09-09
