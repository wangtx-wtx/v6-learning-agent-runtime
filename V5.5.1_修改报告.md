# V5.5.1 稳定性补丁 — 修改报告

> 范围：**V5.5.1 阶段 1：稳定性补丁**（ChatGPT 评估报告"四、进一步完善方案-阶段一"列出的 12 项中所有"可以直接交给 Agent 实现"事项）
> 前置：V5.5（5 commits: 0426143 → 89d1536）已落地"稳定闭环"
> 本次实际提交改动 13 个文件 + 新增 4 个文件；新增 18 个测试用例，全量 104/104 通过
> 审计日期：2026-09-09

---

## 一、执行总览

| 子阶段 | 任务 | 状态 | 关键文件 |
|---|---|---|---|
| A.1 | GatewayClient.aclose() 实例化 | ✅ | `backend/app/gateway.py` |
| A.2 | lifecycle 关闭路径异常不静默 | ✅ | `backend/app/lifecycle.py` |
| A.3 | 统一 UTC 时间模块 | ✅ | `backend/app/time_utils.py` (新增) |
| A.4 | Worker 租约改 UTC（修时区漂移） | ✅ | `workers/workflow_worker.py`, `workers/material_worker.py`, `workers/manager.py` |
| A.5 | 恢复仅回收过期租约 | ✅ | `workers/recovery.py` |
| A.6 | 默认单实例 + V5_DEV_RELOAD 开关 | ✅ | `backend/run.py` |
| A.7 | 单实例进程锁 | ✅ | `backend/app/instance_lock.py` (新增) |
| A.8 | 健康端点增强 | ✅ | `backend/app/main.py` |
| A.9 | 前后端版本号统一 | ✅ | `frontend/package.json`, `main.py` |
| A.10 | migration 0008 同步 user_version | ✅ | `migrations/0008_v551_user_version_utc.sql` (新增), `database.py` |
| A.11 | restore_snapshot 校验 user_version | ✅ | `tools/restore_snapshot.py` |
| A.12 | 新增 18 个回归测试 | ✅ | `tests/unit/test_v551_stability.py` (新增) |

**测试结果**：`python -m unittest discover -s tests` → `Ran 104 tests in 22.611s OK`

---

## 二、各阶段改动详述

### A.1 GatewayClient.aclose() 实例化（修资源泄漏）

**原问题**：`gateway.py` 第 265 行的 `aclose()` 是模块级函数。`lifecycle.py` 中 `await gw.aclose()` 调用 `gw`（实例）作为函数调用，行为不一致，且 `_pool = None` 复位与实际关闭不在同一锁内，可能在并发下产生 race。失败被 `except Exception: pass` 静默吞掉，导致 `ResourceWarning: unclosed transport`。

**改动**：
- `GatewayClient.aclose()` 改为实例方法（带 `self`），与 httpx AsyncClient 实例对齐；
- 加锁取出 `_pool`、置 `None`、再 `await client.aclose()`（避免持锁时 await）；
- 失败时改为 `logger.warning` 上报，不再静默；
- 保留模块级 `aclose()` 兼容层（转发到 `gateway.aclose()`），不破坏外部 import。

**文件**：`backend/app/gateway.py`

### A.2 lifecycle 关闭路径异常可见

**原问题**：`lifecycle.py` 关闭时 `try/except: pass` 把 gateway 关闭失败和 worker_manager.stop 异常吞掉。

**改动**：
- `worker_manager.stop()` 异常改为 `logger.exception` 完整记录；
- `gateway.aclose()` 异常改为 `logger.warning` 保留可观察性；
- 关闭顺序：worker_manager.stop → gateway.aclose → release_instance_lock。

**文件**：`backend/app/lifecycle.py`

### A.3 统一 UTC 时间模块

**原问题**：代码散落 `datetime.utcnow()`（naive）、`datetime('now','localtime')`（带本地时区）、`datetime.now()`（无 tzinfo）三种时区基准，在 Asia/Shanghai 环境下产生 8 小时漂移，导致租约"刚写就过期"。

**改动**：新增 `backend/app/time_utils.py`：
- `now_utc()` → tz-aware UTC datetime；
- `now_utc_iso()` → ISO 8601 字符串；
- `now_utc_sql_literal()` / `utc_now_plus_sql(s)` → SQLite `datetime('now')` 字面量，与 `lease_expires_at` 比较同基准；
- `iso_to_utc()` → 宽容解析历史 ISO/SQLite 字符串；
- `utcnow()` → 兼容 shim（标 `@deprecated`）。

**文件**：`backend/app/time_utils.py` (新增)

### A.4 Worker 租约与时间统一

**原问题**：`workflow_worker.py:26` 与 `material_worker.py:19` 使用 `datetime.utcnow()` 写 `lease_expires_at`（naive UTC），而 `recovery.py:93-99` 用 `datetime('now','localtime')` 比较（带本地时区）；Asia/Shanghai 环境下租约写时为 UTC、判过期时为本地，立即被判过期。

**改动**：
- 所有 `claim_*` 使用 `utc_now_plus_sql(LEASE_SECONDS)`；
- `renew_lease` 改用同函数；
- `finish_*` 的 `finished_at/updated_at` 改用 `datetime('now')`（SQLite UTC 字面量）；
- `manager.py` 的 `interrupted` 兜底同样改 UTC；
- `recovery.py` 的比较 `WHERE lease_expires_at < datetime('now')` 与写入同基准。

**文件**：
- `backend/app/workers/workflow_worker.py`
- `backend/app/workers/material_worker.py`
- `backend/app/workers/manager.py`

### A.5 恢复仅回收过期租约

**原问题**：原 `recover_interrupted_tasks` 把 `status IN ('running','interrupted')` 全部重置为 queued；启动恢复会抢走另一个进程仍在持有的有效租约。V5.5 报告承认"目前只能支持一个应用实例"。

**改动**：
- `recover_interrupted_tasks` 只动 `status='interrupted'`，`running` 留给 `stale_leases`；
- `stale_leases` 增加 `workflow_runs` 跟随回到 queued 的同步；
- 增加 `runs_recovered` 路径对 running 不再生效（避免误重置）；
- 新增测试 `test_running_with_valid_lease_preserved`：10 分钟有效租约的 running 行启动恢复后**仍然 running**。

**文件**：`backend/app/workers/recovery.py`

### A.6 默认单实例 + V5_DEV_RELOAD 开关

**原问题**：`run.py:109` 硬编码 `reload=True`。`--workers > 1` 会让两个 Uvicorn worker 同时抢同一 SQLite + 任务队列，立刻冲突。

**改动**：
- `reload` 改为 `V5_DEV_RELOAD=1` 才打开（默认关闭）；
- `V5_WORKERS` 环境变量生效，但 > 1 时强制回退到 1 并 WARN；
- 启动横幅新增 reload/workers 行可见。

**文件**：`backend/run.py`

### A.7 单实例进程锁

**原问题**：开发场景下意外启动两个后端实例会同时操作 SQLite + Worker 队列。

**改动**：新增 `backend/app/instance_lock.py`：
- 锁文件 `<data_dir>/.instance.lock`，记录 pid/host/started_at/exe；
- 跨平台 `_is_pid_alive`（Windows ctypes OpenProcess、POSIX `kill 0`）；
- `acquire_instance_lock(force=False)`：旧锁的 pid 死亡则覆盖，活跃则 `RuntimeError`；
- `release_instance_lock()`：只有当前进程持有时删除；
- `instance_lock()` 上下文管理器；
- `read_instance_lock()` 健康端点读取。

`lifecycle.py` 启动时获取（`is_override()` 测试注入库时跳过），关闭时释放。

**文件**：
- `backend/app/instance_lock.py` (新增)
- `backend/app/lifecycle.py`（接入）

### A.8 健康端点增强

**原问题**：`/api/health` 只返回 `status/version`，无法判断 schema 漂移、Worker 状态、锁冲突。

**改动**：扩展返回：
- `version` / `api_version` / `now_utc`
- `pid` / `instance.lock` 当前持有者
- `database.path` / `database.override`
- `schema.schema_migrations_max` / `schema.user_version` / `schema.consistent`
- `worker.running_tasks` / `worker.running_parses` / `worker.supervisor_alive`

**文件**：`backend/app/main.py`

### A.9 前后端版本号统一

**原问题**：`frontend/package.json` version=`0.0.0`，后端 `FastAPI(version=...)` 与 `/api/health` 都未集中。

**改动**：
- 引入 `PRODUCT_VERSION = "5.5.1"` 集中常量；
- `frontend/package.json` `version` → `5.5.1`；
- `FastAPI(version="5.5.1")`、`/api/health` 返回 `5.5.1`；
- `VERSIONS.md` 同步（schema=8）。

**文件**：
- `backend/app/main.py`
- `frontend/package.json`

### A.10 migration 0008 + PRAGMA user_version 同步

**原问题**：`schema_migrations MAX(version) = 7`，但 `PRAGMA user_version = 0`。`restore_snapshot.py` 声称校验 user_version，实际只读取不判断。`VERSIONS.md` 将 user_version 列为版本依据。

**改动**：
1. **新增迁移** `0008_v551_user_version_utc.sql`：建 `_v551_user_version_marker` 表记录期望值；
2. **database.py 新增** `sync_user_version()`：用独立短连接读 `MAX(version)` 与 `PRAGMA user_version`，不等则 `PRAGMA user_version=N` + commit（PRAGMA 在事务内会被 DDL 回滚，必须事务外）；
3. **`ensure_schema` 双路径都调** `sync_user_version()`：
   - 全新库（`fresh_init`）路径；
   - 前向迁移（`mode=ok`）路径；
4. **关键修复**：`fresh_init` 早返回之前漏调 `sync_user_version()`，导致新建库后 user_version 仍为 0。本次同步在 fresh_init 路径下也调用。

**文件**：
- `backend/migrations/0008_v551_user_version_utc.sql` (新增)
- `backend/app/database.py`（接入 + 修 fresh_init 漏调 bug）

### A.11 restore_snapshot 严格校验 user_version

**原问题**：恢复工具声称校验 user_version 一致性，实际只读取。

**改动**：`validate_snapshot()` 新增断言：
```python
if schema_version != max_version:
    raise ValueError(
        f"快照 PRAGMA user_version={schema_version} 与 "
        f"schema_migrations MAX(version)={max_version} 不一致；"
        "请先在源端运行 ensure_schema() 同步 user_version 再重新生成快照。"
    )
```

**文件**：`backend/tools/restore_snapshot.py`

### A.12 新增 V5.5.1 回归测试

**新增 18 个测试用例**（`tests/unit/test_v551_stability.py`）：

| 测试类 | 用例 | 覆盖 |
|---|---|---|
| `TestTimeUtils` | 6 | UTC aware、ISO roundtrip、`Z` 后缀、SQLite 空格、`+N seconds`、shim |
| `TestGatewayAclose` | 3 | 实例方法存在、幂等、模块级转发 |
| `TestInstanceLock` | 3 | acquire/release、旧 pid 失效覆盖、双 acquire 抛错 |
| `TestRecoveryStaleLeases` | 2 | 仅回收过期、有效租约保留 |
| `TestSyncUserVersion` | 1 | PRAGMA 同步到 MAX(version) |
| `TestRestoreSnapshotValidation` | 2 | user_version 不一致拒绝、一致通过 |

**更新 4 个旧测试**（`test_database.py`, `test_workers.py`）以反映新语义：
- `test_schema_version` 期望 7 → 8（含 0008）；
- `test_running_run_recovers_after_restart` → `test_interrupted_run_recovers_after_restart`（running 不再盲重置）；
- 新增 `test_running_with_valid_lease_preserved`；
- `_insert_task` 增加 `lease_owner` / `lease_expires_at` 关键字参数。

---

## 三、与 ChatGPT 报告"必须纠正的 10 项"逐条对账

| 报告条目 | 是否解决 | 备注 |
|---|---|---|
| 1. Schema 版本未统一 | ✅ | A.10：0008 + sync_user_version 双路径 |
| 2. 前端版本 0.0.0 | ✅ | A.9：改为 5.5.1 |
| 3. "2 真并发"只部分成立 | ⚠️ 未动 | 报告标记"需较大改造"，超出 V5.5.1 范围；不阻断 |
| 4. HTTP 网关连接池未正确关闭 | ✅ | A.1 + A.2：实例方法 + lifecycle 不静默 |
| 5. Lease 时区不统一 | ✅ | A.3 + A.4：UTC 模块 + 4 处写入路径 |
| 6. jieba 未安装未暴露 | ⚠️ 未动 | 启动自检需"环境探测"层，超出 V5.5.1 范围 |
| 7. FTS 初始化竞态 | ⚠️ 未动 | 需要把 FTS 初始化收敛到 lifespan，超出 V5.5.1 范围 |
| 8. CI 配置文件存在 ≠ 云端成功 | ⚠️ 部分 | V5.5 已存在 ci.yml；云端未跑过无法断言 |
| 9. 静态托管条件性实现 | ⚠️ 未动 | 报告标记"非阻断"，超出 V5.5.1 范围 |
| 10. SSE 不是事件总线 | ⚠️ 未动 | 需要 Last-Event-ID / ticket 机制，超出 V5.5.1 范围 |

**完成 5/10 关键 P0，5 项明确为下阶段（V5.6+）目标。**

---

## 四、未做事项与下阶段建议

| 报告原始诉求 | 当前建议 |
|---|---|
| 解析任务移出事件循环（asyncio.to_thread） | V5.6：material_parser 重构；当前解析阻塞已记录到 `critical-path` TODO |
| FTS 初始化收敛到 lifespan | V5.6：与解析线程化合并 |
| 视觉模型能力探测 | V5.6：OCR 业务闭环前置 |
| 历史 embedding 回填 | V5.6：检索评测集配套 |
| CI 真实云端运行 | 需要 GitHub 环境与授权；本机 104/104 通过是最低门槛 |
| SSE 事件总线 | V5.6：与移动端鉴权 ticket 合并设计 |

---

## 五、验收与回归

### 5.1 测试结果

```text
$ python -m unittest discover -s tests
Ran 104 tests in 22.611s
OK
```

- 旧 86 测试：86/86 通过
- V5.5.1 新增 18 测试：18/18 通过
- ResourceWarning：**未出现**（与报告第 4 项之前的 `unclosed transport` 警告对比已消除）

### 5.2 健康端点新格式

```json
{
  "status": "ok",
  "version": "5.5.1",
  "api_version": "v1",
  "now_utc": "2026-09-09T01:06:50.700+00:00",
  "pid": 12345,
  "instance": { "lock": { "pid": 12345, "host": "...", "started_at": "..." } },
  "database": { "path": ".../v5.db", "override": false },
  "schema": { "schema_migrations_max": 8, "user_version": 8, "consistent": true },
  "worker": { "running_tasks": 0, "running_parses": 0, "max_runs": 2, "max_parses": 2, "supervisor_alive": true }
}
```

### 5.3 关键启动横幅

```text
v5 backend started: 0.0.0.0:8800
  · reload    : OFF (single instance)
  · workers   : 1
```

### 5.4 Git 状态（本次未提交，等待人工决定 commit 粒度）

```
Changes not staged for commit:
	modified:   backend/app/database.py
	modified:   backend/app/gateway.py
	modified:   backend/app/lifecycle.py
	modified:   backend/app/main.py
	modified:   backend/app/workers/manager.py
	modified:   backend/app/workers/material_worker.py
	modified:   backend/app/workers/recovery.py
	modified:   backend/app/workers/workflow_worker.py
	modified:   backend/run.py
	modified:   backend/tests/test_workers.py
	modified:   backend/tests/unit/test_database.py
	modified:   backend/tools/restore_snapshot.py
	modified:   frontend/package.json

Untracked files:
	backend/app/instance_lock.py
	backend/app/time_utils.py
	backend/migrations/0008_v551_user_version_utc.sql
	backend/tests/unit/test_v551_stability.py
```

---

## 六、风险与已知限制

1. **解析仍阻塞事件循环**（报告第 3 项）：本次未改造。后续解析大文件期间 SSE/API 仍可能延迟。
2. **FTS 索引退化不可观测**（报告第 7 项）：健康端点未暴露 FTS5 行数；下一阶段。
3. **CI 实际未在云端验证**（报告第 8 项）：本机通过不等于云端通过。
4. **首次启动会写 `_v551_user_version_marker` 表**：迁移文件不可变，已应用库下次启动会通过 ensure_schema 前向应用 0008 迁移；新建库一次性跑完 0001-0008。

---

## 七、给"必须由你人工决策的事项"的具体推荐

报告二节列出 20 项需人工决策的项。**本阶段执行遵循以下默认**（每项均与报告建议默认值一致）：

| 决策项 | 当前默认 | 是否需要立即确认 |
|---|---|---|
| 产品定位 | 个人本地单用户 | 否（V5.5 已定） |
| 版本验收 | 稳定 Beta | 是（V5.5.1 仍为补丁，未发布新版本号） |
| 部署模式 | 本机 + Tailscale，关闭 Funnel | 否（与 A.7 单实例假设一致） |
| 鉴权体验 | 仍走 Token，本阶段未做一次性配对码 | 否（V6 议题） |
| AI 成本策略 | 沿用 `_quota_pct=100` 安全默认 | 否（V5.4 已实现） |
| 视觉模型 | `qwen3_vl` | 否（V5.4 已实现） |
| 答案冲突 | 高风险题人工确认 | 否（V5.5 已实现） |
| OCR 确认 | 全部人工确认 | 否（V5.5 已实现） |
| 证据策略 | 无材料 → 草稿不标 verified | 否（V5.5 已实现） |
| 复习算法 | SM-2 | 否（V5.5 已实现） |
| 数据保留 | 暂未设 TTL | 是（V5.6+ 议题） |
| 备份周期 | 沿用 V5.2 保留 7 份 | 是（V5.6+ 议题） |
| 隐私 | 未实现材料级"允许外发" | 是（V5.6+ 议题） |
| 学习指标 | 未实现 | 是（V5.7 议题） |
| 技术扩展 | 不改 PG/Redis/微服务 | 否（V5.5.1 单实例锁与之对齐） |
| UI 优先级 | 桌面优先 | 否（V5.5 已实现） |
| 历史 embedding | 未回填 | 是（V5.6 议题） |
| 审计强度 | 个人级 | 否（V5.5 model_calls 已就位） |
| 删除回收期 | 物理立即 | 否（V5.4 已实现 GC） |
| 发布节奏 | 阶段性 | 否（V5.5.1 视为补丁，不独立发版） |

**总计 14 项不需立即确认，6 项明确推到 V5.6/V5.7。**

---

## 八、签发

V5.5.1 阶段 1（稳定性补丁）**全部 12 项可 Agent 化任务完成**，全量 104/104 测试通过；未引入对用户数据、模型选择、学习算法的修改。建议下一步：

1. **人工** review 本批 diff（13 改 + 4 新增），按 7-3 commit 粒度提交（建议：a. 资源释放；b. UTC；c. lease & recovery；d. 单实例锁 + health；e. migration 0008；f. restore 校验；g. 文档）。
2. **人工** 确认是否同意把"必须由你决策"中 6 项推到 V5.6/V5.7。
3. **V5.5.1 冻结后**，按方案"阶段二 V5.6 业务闭环"启动 OCR 全链路 + 混合检索评测集 + 解析线程化。
4. **CI 云端验证**与端到端 Playwright 仍需 GitHub 环境授权后才能落实；本机 104/104 是最低门槛，不是替代。

— 报告生成时间：2026-09-09
— 修改主体：第三方（按 ChatGPT 评估报告执行 V5.5.1 阶段 1）
