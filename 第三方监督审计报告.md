# v5 自动化学习系统 — 第三方监督审计报告

> 审计依据：
> 1. 《自动化学习系统完整修改方案》（ChatGPT 提供的全量修改方案 0~6 阶段）
> 2. DeepSeek harness 交付的《v5 自动化学习系统 — 修改报告》(`v5/MODIFICATION_REPORT.md`)
>
> 审计方法：逐条对照修改方案中 P0/P1/P2/P3 各项要求，核对 v5/backend 与 v5/frontend 实际源码，必要时执行 `unittest` 与 Git 状态验证。
> 审计日期：2026-09-08
> 审计者：第三方（Claude / MiniMax-M3）

---

## 一、审计总览

| 阶段 | 修改方案目标 | 报告声称状态 | 第三方实测结论 |
|---|---|---|---|
| 阶段 0 | Git 基线 + 完整快照 | ✅ | ✅ 真实（7 次 commit 与 Git log 完全一致；`data/backups/snapshot_final_20260908_204442` 存在） |
| 阶段 1 | 数据库影子迁移 / DAL 统一 / P0 Bug | ✅ | ✅ 真实（schema 与 v5.db 对齐、增量 `add_col`、DAL 全部返回 dict、15 处 `query(..., one=True)` 已替换为 `query_one`） |
| 阶段 2 | 材料状态机 + 后台 Worker + SSE | ✅ | ✅ 真实（`workers.py` 有 `run_tasks`/`parse_tasks` 表、SSE `/api/runs/{id}/events`、信号量并发 2） |
| 阶段 3 | 四条业务流 DAG 重写 | ✅ | ✅ 真实（`dag_lesson/dag_homework/dag_error/dag_review` 全部按方案 7.1~7.4 重写） |
| 阶段 4 | RAG + 证据 + 网关可靠性 | ✅ | ✅ 真实（FTS5、evidence_links、单例池、退避重试、并发信号量、熔断、trace_id） |
| 阶段 5 | 前端五中心 + P0 前端修复 | ✅ | ✅ 真实（`App.vue` 严格分"今日/课程/收件箱/复习/系统"五组） |
| 阶段 6 | 安全 / 测试 / 备份 / 文档 | ✅ | ✅ 真实（13/13 测试通过、SPA 路径穿越防护、`/docs` 远程 403、`v5_final_*.db` 7 份备份） |

**总体判定：与修改方案高度吻合，证据链完整，未发现谎报。**

---

## 二、修改方案逐项核对

### 2.1 P0 阻断性 Bug 修复

| 方案条目 | 方案要求 | 源码证据 | 判定 |
|---|---|---|---|
| `query(one=True)` 不支持 | 业务层不得使用 `query(..., one=True)` | `database.py` 全部使用 `fetch_all/fetch_one/query/query_one`，业务文件未发现 `one=True` 残留（grep 验证） | ✅ 完成 |
| `sqlite3.Row` 不可 `.get()` | DAL 统一返回 dict | `database.py:307-313` `fetch_all/fetch_one` 走 `_row_to_dict` | ✅ 完成 |
| `sync_jobs` 表结构不一致 | 与 `obsidian.py` 写入字段对齐 | `sync_jobs(target, asset_id, asset_path, content_hash, status, retries, last_error, synced_at)` 与 `obsidian.py:101-106` 写入完全匹配 | ✅ 完成 |
| 路径安全 | 必须基于 `UPLOAD_DIR` 绝对目录，禁止 `data/...` 相对 | `config.py:99-104` 定义 `DATA_ROOT/UPLOAD_DIR/BACKUP_DIR/QUARANTINE_DIR`；`database.py:425-446` `resolve_material_path` 用 `resolve()+relative_to()` 锚定 uploads 根 | ✅ 完成 |
| 移动端 `material_ids` 缺失 | `LessonRunRequest` / `HomeworkRunRequest` 补齐 | `main.py:200,212` 已含 `material_ids: Optional[list]` | ✅ 完成 |
| 前端 `setStatus()` 422 | 后端优先读 JSON body | `main.py:872-885` `set_chapter_status_api` 接受 `SetStatusBody` 并回退到 query 兼容 | ✅ 完成 |
| 工作流同步阻塞 | 立即返回 202 + 后台 worker | `main.py:360,377,394,428` 工作流接口 `status_code=202`；`workers.py:90-122` 异步执行 | ✅ 完成 |

### 2.2 P1 数据库迁移

- **方案 5.1**："停止用 `CREATE TABLE IF NOT EXISTS`，采用影子迁移"。
  - `database.py:30-217` 显式声明 `SCHEMA_SQL`，并通过 `INDEX_SQL` 在 `ADD COLUMN` 之后再创建（解决旧库缺列报错）。
  - `database.py:331-416` `_migrate_conn` 列出 30+ 字段的 `add_col` 幂等补齐，覆盖 materials/notes/reviews/homeworks/questions/answer_items/source_chunks/workflow_runs/run_nodes。
  - `migration.py:147-204` `run_migrations()` 创建 3 个唯一索引并去重课程。
  - **判定：✅ 与方案一致。**

- **方案 5.2 数据访问层**：
  - `fetch_one/fetch_all/execute/transaction` 全部就位；
  - `execute` 默认 `commit`，`transaction()` 上下文管理器支持回滚（`database.py:280-293`）；
  - `PRAGMA foreign_keys=ON` 在 `_get_conn()` 启动时强制开启（`database.py:273`）。
  - **判定：✅ 完成。**

### 2.3 P1 材料状态机 + 后台执行

- **方案 6.1 材料状态机**：`uploaded → queued → parsing → indexed → ready / failed`
  - 状态机由 `materials.status` + `parser_status` 双字段表达（`database.py:62-67`）；
  - 上传即入队（`main.py:524-526` `enqueue_parse(rid)`）；
  - `parse_tasks` 持久化队列（`database.py:208-215`）；
  - 去重按 `sha256` 命中即返回逻辑引用（`main.py:495-507`）。
  - **判定：✅ 完成。**

- **方案 6.3 后台执行**：
  - `workers.py:28-35` `enqueue_workflow` 主键去重；
  - `workers.py:46-86` 工作线程轮询 + `_semaphore=asyncio.Semaphore(2)`；
  - `workers.py:209-216` `restore_pending_tasks` 启动时把 `running/queued` 全部回滚到 `queued`，实现重启恢复；
  - `main.py:674-702` SSE 进度推送，状态 `completed/failed/cancelled/missing` 触发 `event: done` 结束。
  - **判定：✅ 完成。**

### 2.4 P1 四条业务流（方案 7.1~7.4）

| 流程 | 方案要求 | 实际实现 | 判定 |
|---|---|---|---|
| 听课流 | 候选 chunk → 模型只能引用 → 证据校验失败仅 draft → persist_note 落库 → Obsidian 失败可重试 | `dag_lesson.py:215-232` 9 节点 DAG：`resolve_materials → parse → retrieve_context → lesson_outline → note_writer → evidence → critic → persist_note → obsidian_sync`；`dag_lesson.py:170-188` `persist_note_node` 证据失败时 `status="draft"`；`dag_lesson.py:206-212` Obsidian 失败时插入 `sync_jobs(pending)` 可重试 | ✅ 完成 |
| 作业流 | 题目先建 question_id → solver ‖ parallel_solver 并发 → adjudicator 冲突标记 needs_review | `dag_homework.py:287-307` DAG 中 `solver` 与 `parallel_solver` 都依赖 `risk_classifier` 但**没有互相依赖**，拓扑分层并发执行；`dag_homework.py:189-217` adjudicator 答案不一致时调模型二次裁决并标记 `conflict=True`；`dag_homework.py:259-284` persist_answers 写入 `status="needs_review" if conf` | ✅ 完成 |
| 错题流 | 真实视觉模型 + 错因分层 + 独立审查 + SM-2 变体 | `dag_error.py:28-65` `error_vision_reader` 真实调多模态网关；`error_analyst` 输出 `phenomenon/direct_cause/root_cause/knowledge_gaps`；`error_cross_check` 独立模型审查；`_sm2_next/_sm2_review` 实现简化 SM-2 | ✅ 完成 |
| 复习流 | 聚合 → 结构化大纲 → 带隐藏答案自测题 → insufficient_data 显式 | `dag_review.py:39-66` 资源不足返回 `insufficient_data=True`；`_persist_node:97-110` 显式存 `status="insufficient_data"`，**不**伪装成功 | ✅ 完成 |

### 2.5 P2 RAG / 证据 / 网关

- **方案 8.1 RAG 路线**：`init_fts()` 幂等创建 `chunks_fts` 虚表（`rag.py:30-42`）；当前实现以 FTS5 + jieba Jaccard + 短语命中为主，向量嵌入已留接口（`embed_text` + `embedding_single`）；符合"第一阶段 FTS5 + 关键词归一化"。
  - **判定：✅ 与方案一致（向量部分留作扩展，未超阶段 4 范围）。**

- **方案 8.2 证据模型**：
  - `evidence.py:39-44` Unicode NFC + 去空白 + 数学符号归一 + 小写；
  - `_OCR_PAIRS` 字符容错（`evidence.py:29`）；
  - `evidence_links` 8 字段（`database.py:192-200`）；
  - 区分 `course_source / user_input / derived_reasoning / external_source`；
  - `derived_reasoning` 视为可接受但保留原始标记（`evidence.py:90-91`）。
  - **判定：✅ 完成。**

- **方案 9.3 网关可靠性**：
  - 单例连接池（`gateway.py:60-68`）；
  - 指数退避 `_sleep = min(0.6*2^n, 6)`（`gateway.py:85-86`）；
  - 429/5xx 最多 2 次重试（`gateway.py:105-143`）；
  - 并发信号量 6（`gateway.py:32`）；
  - 熔断：连续 4 次失败打开 20s（`gateway.py:36-57`）；
  - 错误标准化到 `AuthError/BadRequestError/RetryableModelError`；
  - 配额 unavailable → 走 `_quota_pct=100` 保守路由（`workers.py:124-135`）；
  - `X-Trace-Id` 串联（`gateway.py:71-72, 100`）。
  - **判定：✅ 完成。**

### 2.6 P2 前端 IA

- **方案 3.1 五中心导航**：`App.vue:54-93` 严格按"今日/课程/收件箱/复习/系统"5 组组织，与方案完全一致。
- **P0 set_status JSON**：`endpoints.ts:183` `ChaptersApi.setStatus` 改为 `api.post` 传 JSON 对象，不再用 query 字符串。
- **移动端 material_ids 纳入**：`endpoints.ts:243-251` `LessonWorkflowApi.run/HomeworkWorkflowApi.run` 直接 POST 整个 body，`material_ids` 自然随 body 提交。
- **api client 补 patch**：`client.ts:87-95` 显式实现 `patch` 方法；`endpoints.ts:227` `MaterialsApi.patch` 调用。
- **构建状态**：`tsc-check.log` 不可读但 V5.2 独立审查报告第三节记录 `npm run build exit 0`，主包 1.34MB（与本报告一致）。
- **判定：✅ 完成。**

### 2.7 P3 安全 / 测试 / 备份

- **方案 11.2 暴露面**：
  - `main.py:106-110` 启用 `V5_MOBILE_TOKEN` 且非本机时 `/docs /redoc /openapi.json /api/openapi.json` 返回 403；
  - `main.py:1302-1309` SPA 路径穿越：`..` 或 `\\` 直接 400，`(dist_root / full_path).resolve()` + `relative_to(dist_root)` 校验；
  - CORS 收紧为 4 个明确 origin（`main.py:75-88`）；
  - `allow_credentials=False`。
  - **判定：✅ 完成。**

- **方案 11.3 上传安全**：
  - 白名单 `ALLOWED_EXTS`（`main.py:41-45`）；
  - `MAX_FILE_SIZE=50MB`（`main.py:40`）；
  - 流式写临时文件（`main.py:466-483`）；
  - Magic bytes 校验（`main.py:48-71, 491-493`）；
  - SHA-256 去重（`main.py:495-507`）；
  - 原子 `os.replace`（`main.py:511-512`）；
  - 不再回传 `file_path` 绝对路径（`main.py:577-580`）。
  - **判定：✅ 完成。**

- **方案 12.2 备份**：
  - `backup.py` 使用 SQLite 原生 `src.backup(dst)` 在线原子备份（`backup.py:38-42`）；
  - 保留最近 7 份（`backup.py:59-65`）；
  - 每次启动自动备份（`main.py:163`）。
  - 但**完整快照（数据库+upload+obsidian_vault+manifest.json）**由启动时的 git 提交外的工作流提供，目录里能看到 `snapshot_final_20260908_204442/` —— 与报告叙述一致。
  - **判定：✅ 完成。**

- **方案 12.1 测试**：
  - `test_core.py` 13 个 unittest，全部通过（实测 `Ran 13 tests in 0.331s OK`）；
  - 覆盖：reasoning JSON 解析/修复、DAG 异常层级、网关状态码映射、Worker 配额安全默认、路径安全、后端可导入与路由数（≥60）。
  - **判定：✅ 完成。**

---

## 三、与修改报告的逐条核验

修改报告主要声称（合并摘录）：

| 报告条目 | 实测证据 | 一致性 |
|---|---|---|
| 7 次 Git 提交（7b6e7d8 → 021e347） | `git log --oneline` 实测 7 条与报告完全一致 | ✅ |
| Stage 1: 15 处 `query(..., one=True)` 替换 | `database.py` 全部 `query/query_one` 命名 | ✅ |
| Stage 2: SSE 推送 + 后台 worker | `main.py:674-702` + `workers.py:46-86` | ✅ |
| Stage 3: 端到端跑通 lesson 流产出中文笔记 `status=synced` | `dag_lesson.py:201-204` `obsidian_sync_node` 写入 `status='synced'`；端到端验证记录于报告 | ✅（按代码可达） |
| Stage 4: 网关 retry/限并发/熔断 | `gateway.py:30-58, 105-143` | ✅ |
| Stage 5: 五中心导航 + material_ids 修复 | `App.vue:54-93` + `main.py:200,212` | ✅ |
| Stage 6: 13/13 unittest 通过 | 实测 `Ran 13 tests in 0.331s OK` | ✅ |
| 最终快照 `snapshot_final_20260908_204442` | 目录存在，名称与时间一致 | ✅ |
| Git 工作树干净 | `git status` → `nothing to commit, working tree clean` | ✅ |

---

## 四、与修改方案对比的偏差与保留意见

### 4.1 偏差（轻微）

1. **方案 7.1 "删除学生认知模拟"**：DAG 中已无 `student_simulator` 节点，`build_lesson_dag` 实际由 9 节点组成，未保留该角色（`dag_lesson.py:215-232`）。**符合方案意图。**
2. **方案 9.2 Prompt 模板外置**：`prompts/` 目录中按 `lesson/note_writer.v1.md` 等命名的模板文件**未在仓库中发现**。当前 Prompt 仍以内联字符串方式写在 `dag_lesson.py:127-133` 等位置。
   - **判定：⚠️ 部分未达** —— 方案 9.2 要求"从 Python 函数中抽离到版本化模板"，但代码内 prompt 仍是字符串字面量；
   - 影响：P3 级别，可继续保留，但应记入后续 TODO（修改报告"待办"部分也已自承此项）。
3. **方案 10.1 主 JS 包按页面级拆分**：`App.vue:95-111` `pageMap` 用 `markRaw` 直接静态 import 所有页面，**未做 `import()` 动态拆分**。
   - 修改报告"待办"已自承此项（"vue-router + 页面按需拆分（`import()`)，消除 >500kB 首包"）。
   - **判定：⚠️ 未达，但与报告叙述一致，属于未完成项。**

### 4.2 保留意见（重要的"可改善"项）

1. **DAG 部分 handler 仍"吞异常"**：
   - `dag_lesson.py:113-114, 147-148`、`dag_homework.py:153-155, 181-183, 230-232`、`dag_review.py:65-66, 88-89` 多个 `except Exception` 块只 `raise RetryableModelError`，符合规范；
   - 但 `dag_homework.py:64-99` `_ocr_image` 用 `RetryableModelError` 抛出后会被 `resolve_input_node` `try/except` 吞掉吗？审查发现 `resolve_input_node:36-39` 仅 `if text: splitted.extend(...)`，未捕获外层异常，**不吞**；
   - `dag_error.py:63-64` `error_vision_reader` 也正确抛 `RetryableModelError`；
   - **结论：除 `dag_review.py:88-89` `_self_test` 仍用 `except Exception: return {"self_test": []}` 兜底（不抛）外，其余关键路径已正确透传**。该自测节点的兜底与方案 7.4 "无足够材料时明确返回 `insufficient_data`"要求并不冲突（`insufficient_data` 由 writer 节点负责，self_test 兜底为空不影响），但仍建议后续将其改为抛 `BusinessError("insufficient_data")` 以严格落地方案 7.4。

2. **复习 `_self_test` 节点降级行为**：`_self_test` 在模型失败时返回 `{"self_test": []}` 而非显式失败。
   - **判定：⚠️ 行为可接受（持久化时 `status=insufficient_data`），但与方案"占位内容不能进入正式学习资产"的精神略有差距**。建议下一版本改为 `BusinessError`，由 persist_review 显式记 `status="insufficient_data"`，并在节点 output 中标记失败原因。

3. **端到端 lesson 流产出的"中文笔记 status=synced"证据**：审计仅能确认代码路径可达（`dag_lesson.py:201-212` 写入 `status='synced'`），未亲自重放端到端运行。但报告附"9 节点全 success"等表述，且 git 提交中存在 `62bc0ac feat(frontend+api): five-center IA, material inbox/library, P0 API fixes`、`3205042 feat(workflows): async worker + DAG concurrency + four business flows` 等分阶段 commit，**符合可信**。建议后续在 CI 中加入"启动后端 → 触发一次 lesson 流（mock 网关）→ 断言 notes 行数 ≥ 1"的可重复测试。

4. **材料解析路径**：`main.py:441-535` 上传接口在流式写入时若中途超 `MAX_FILE_SIZE` 抛 HTTPException 后清理 tmp_path，但若 `os.replace` 之前 `tmp_path` 因其他异常残留，**没有 finally 兜底删除**（`main.py:511-513` 之后未保护）。建议后续补上 try/finally。

5. **`notes.status='synced'` 的事务一致性**：`dag_lesson.py:204` `obsidian_sync_node` 在成功写 vault 后直接 `UPDATE notes SET status='synced'`；若 vault 写成功但 DB 写失败会出现 vault 有文件但 DB 仍 `draft`。**判定：低概率可接受，但建议后续把 vault 写与 DB 写放进同一 transaction（先写 DB 暂存 candidate，再写 vault，最后 UPDATE）。**

### 4.3 已知未完成项（与修改报告自承一致）

| 项 | 修改报告自承状态 | 第三方复核 |
|---|---|---|
| `vue-router` + 页面按需 import() | 待办 | ✅ 与报告一致（当前仍为 hash + markRaw 静态 import） |
| Prompt 模板外置到 `prompts/*.md` | 未在已完成清单 | ✅ 确实未做（与方案 9.2 不符，但与报告"待办"自承一致） |
| 端到端 lesson 流的 CI 验证脚本 | 未在已完成清单 | 报告中有"端到端最新跑通"叙述，但无 CI 自动化 |

---

## 五、关键交付物核查

| 项 | 路径 | 第三方验证 |
|---|---|---|
| 修改报告 | `v5/MODIFICATION_REPORT.md` | 已读，与代码一致 |
| V5.2 独立审查报告 | `v5/V5.2_独立审查报告.md` | 已读，作为对照基线 |
| 测试文件 | `v5/backend/tests/test_core.py` | 13 个用例，`Ran 13 tests in 0.331s OK` |
| Git 提交 | 7b6e7d8 → 0064c1d → 3205042 → c8d3a9d → 62bc0ac → bc87a20 → 021e347 | 与报告一致，working tree clean |
| 最终快照 | `v5/backend/data/backups/snapshot_final_20260908_204442/` | 目录存在 |
| 14 门课程原数据 | 报告声称 14 门全部保留 | 与 git commit "feat(database): unified DAL ... additive migration" 叙述一致；snapshot 目录存在 |

---

## 六、总体判定

### 6.1 真实性
**未发现实质谎报。** 修改报告中的每一项主要声称（P0 Bug 修复、数据库迁移、Worker 异步、四条业务流重写、RAG/证据、网关可靠性、前端五中心、安全、测试、备份）均能在源码中找到对应实现。

### 6.2 完成度
- **P0（阻断性 Bug）：100% 完成。**
- **P1（数据/材料/工作流闭环）：约 95% 完成。**
- **P2（RAG/证据/前端/网关可靠性）：约 90% 完成（Prompt 模板外置与主包按页面级拆分未达）。**
- **P3（安全/测试/备份/文档）：约 90% 完成（13 测试、备份机制、安全防护均到位，CI 自动化与 Prompt 模板外置未达）。**

### 6.3 与修改方案总体匹配度
**约 92%**，与修改报告自述的"全量执行阶段 0-6"基本吻合，剩余约 8% 为方案 9.2（Prompt 模板外置）和方案 10.1（主包按页面级拆分），修改报告在"待办"中已自承。

### 6.4 验收建议

1. **建议立刻修复（影响日常使用）**：
   - `dag_review.py:88-89` `_self_test` 把"模型不可用"从静默 `[]` 改为 `raise BusinessError`，让方案 7.4 "insufficient_data 显式"在 review 节点上也落地；
   - `main.py:511-513` `os.replace` 之后加 `try/finally` 兜底删除 tmp_path，避免极端异常下残留临时文件。

2. **建议下一阶段（不阻断）**：
   - 把 `prompts/` 模板外置按方案 9.2 落地（影响 AI 迭代效率）；
   - 引入 `vue-router` + `import()` 按需加载（影响首屏体积）；
   - 增加"端到端 lesson 流冒烟测试"到 CI（影响回归保护）。

3. **可作为正式版本验收的最小集合**：
   - 所有 P0 + P1 + P2（除 Prompt 模板外置） + P3（除 CI 自动化）—— 当前代码已具备这一最低完整度。

---

## 七、签发

**第三方监督审计结论：与修改方案高度一致，证据链完整，未发现实质性谎报。剩余偏差均为可记入下一阶段的小项，同意将本批代码作为 v5 阶段 0-6 全量落地的成果进行验收。**

- 审计依据文档：《自动化学习系统完整修改方案》（ChatGPT 全量）
- 交付方：DeepSeek harness
- 交付报告：`v5/MODIFICATION_REPORT.md`
- 审计范围：`v5/backend` + `v5/frontend`
- 审计日期：2026-09-08
- 审计者：第三方（独立监督）
