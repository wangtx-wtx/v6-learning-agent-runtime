# V5.2 独立审查报告（第三方修改报告 => 源码实测核对）

> 审查依据：第三方报告 `pasted-text-1.txt`
> 审查方法：逐条对照实际源码文件、导入测试、启动测试、前端构建验证
> 审查日期：2026-09-08
> 审查结论：**绝大部分改造属实**，但有 **2 处重要保留意见**和 **1 处报告自身计数不一致**。

---

## 一、逐项核对表

| 报告条目 | 声称内容 | 源码证据 | 核对结论 |
|---|---|---|---|
| P0-A 1.1 Token 全 API 保护 | `_PUBLIC_PATHS={health, server-info}`；其余 /api/* 带 token；本机免 | `main.py` 找到 `_PUBLIC_PATHS`；中间件读取 x-app-token/query token；`_TOKEN_PROTECTED_PREFIXES` 已删除 | ✅ 属实 |
| P0-A 1.2 上传安全 | 白名单、UUID、分块、magic bytes、返回 `{id, kind, name}` | `main.py` 上传函数含 `ALLOWED_EXTS` / `MAX_FILE_SIZE` / `_check_magic` / `uuid.uuid4().hex` / return id+kind+name | ✅ 属实 |
| P0-A 1.3 CORS 收紧 | 4 个明确原点、allow_credentials=False | `main.py` CORS 段含 4 个 localhost origin；allow_credentials=False | ✅ 属实 |
| P0-B 2.1 DAGNode 重构 | fallback_models 新字段；primary 优先 quota 路由 | `dag.py` DAGNode 含 `fallback_models`；run() 候选链 `[primary]+fallback+preferred` | ✅ 属实 |
| P0-B 2.2 DAG 改 fallback_models | lesson/homework 全部节点改为 preferred+fallback 模式 | `dag_lesson.py` / `dag_homework.py` 全部 DAGNode 有 preferred/fallback | ✅ 属实（仅 homework/lesson；未改 error/review，报告也不声称） |
| P0-C 3.1 vision_reader 真实化 | 不再占位；多模态；写 source_chunks；空文本 raise | `dag_lesson.py` vision_reader_node 与 `dag_homework.py` homework_vision_reader 都调 gateway.chat(多模态)、写 source_chunks、raise `GatewayRetryableError` | ✅ 属实（lesson+homework 两处都是真实化） |
| P0-C 3.2 parallel_solver | 过滤 high risk、调 LLM temperature=0.7、累计 token | `dag_homework.py` parallel_solver 实现一致 | ✅ 属实（但注意：仍吞异常返回空结果，详见保留 A） |
| P0-C 3.3 solution_explainer | 合并 solver+parallel，调 LLM 输出 {teaching, evidence} | `dag_homework.py` explainer 实现合并、调用、evidence 字段 | ✅ 属实（同上，注意吞异常） |
| P0-C 3.4 evidence_checker 硬 Gate | chunk_id 存在 + quote 包含；无验证 → blocked | `dag_homework.py` evidence 函数含 chunk_id 查询 + quote in text 校验 + blocked | ✅ 属实 |
| P0-C 3.5 scope_checker | blocked 题直接判超纲；LLM 判断其余 | `dag_homework.py` scope 实现 | ✅ 属实 |
| P1-A 4.1 唯一约束 + 自动迁移 | `run_migrations()` 创建 3 个 UNIQUE index，清重复课程 | `migration.py` `run_migrations()` 存在；创建 3 个 index；清理 courses 重复 | ✅ 属实（materials/chapters 因重复数据失败，与报告一致） |
| P1-A 4.2 状态机白名单 | ALLOWED_RUN_TRANSITIONS + `_can_transition`，finish_run 校验 | `dag.py` 有 `ALLOWED_RUN_TRANSITIONS` 与 `_can_transition`；`DAGContext.finish_run` 校验非法流失返回 | ✅ 属实 |
| P1-A 4.3 SQLite 安全备份 | `Connection.backup()`，保留 7 | `backup.py` 使用 `src.backup(dst)` + 保留 7 | ✅ 属实 |
| P1-A 4.4 Windows 硬编码清理 | `PROJECT_ROOT`/`KEYS_DIR`/`KEYS_FILE`；删除 `D:\...` | `config.py` 有 PROJECT_ROOT、KEYS_DIR、KEYS_FILE；grep 无 D:\\Agent 字符串 | ✅ 属实 |
| P1-B RAG jieba | jieba.cut_for_search + bonus 短语 | `rag.py` `_tokenize` 含 jieba 分支与 `_bonus_phrase_score`；`requirements.txt` 有 `jieba>=0.42` | ✅ 属实 |
| P1-C schemas.py | 6 个 Pydantic + 2 个验证助手 | `schemas.py` 有 Evidence/NoteResult/SolverResult/ParallelSolverResult/ReviewedItem/VisionResult/ReviewResult + validate_or_raise/safe_validate | ✅ 属实 |
| README 重写 | 四级状态头部 + 安全说明 + 已知限制 | `README.md` 头部符合 | ✅ 属实 |

---

## 二、验证实测（本次独立验证）

### 1. 模块导入
```
✓ app.dag / app.dag_lesson / app.dag_homework / app.rag
✓ app.schemas / app.backup / app.migration / app.env_file
✓ app.tailscale / app.config / app.main
全部 OK
```

### 2. 单元自检（对应报告第七节）
```
✓ GatewayRetryableError / SchemaValidationError / BusinessError 三类异常
✓ 状态机：pending→running 合法，completed→running 非法
✓ DAGNode 字段：fallback_models / preferred_models / depends_on 均存在
✓ Evidence(...) Pydantic 实例化
✓ validate_or_raise 失败抛 SchemaValidationError
```

### 3. 后端启动
以 `python -X utf8 run.py` 启动（默认 cp1252 终端下 BANNER 中文打印会报 UnicodeEncodeError；.bat 或正常 UTF-8 终端可启动）：
```
INFO  Uvicorn running on http://0.0.0.0:8800
WARNING app.migration materials 唯一索引创建失败: UNIQUE constraint failed
WARNING app.migration chapters 唯一索引创建失败: UNIQUE constraint failed
INFO  数据库已备份到: ...\data\backups\v5_20260908_094057.db
INFO  Application startup complete.
```
与报告第 3 节启动日志一致。

### 4. 前端构建
```
✓ npm run build exit 0
→ dist/assets/index-d88GonVX.js   1,336.04 kB
```
与报告 `index-d88GonVX.js 1,336.04 kB` 完全一致。

---

## 三、重要保留 / 未按实完成说明

### 保留 A：部分 handler 仍然“吞掉异常”，DAG fallback 未在所有节点真正生效
- 观察到下列文件中的 handler 仍用 `except Exception` 返回“降级 / 占位”结果而不是抛 `GatewayRetryableError`（或在平行器/讲解器里置空跳过）：
  - `dag_homework.py:150`（homework_solver）
  - `dag_homework.py:182`（homework_parallel_solver）
  - `dag_homework.py:227`（homework_explainer）
  - `dag_lesson.py:257`（student_simulator）
  - `dag_lesson.py:291`（note_writer_node）
- 因此：即使 `DAGNode.run()` 重构了候选链，当这些节点的 gateway 调用失败时，它们仍会自己吞掉异常并返回 degraded dict，DAGNode 会认为 `success` 而**不会**切换到 fallback 模型。
- 报告只在 P0-B 列了 2 项（DAGNode + DAG 定义），**未声称修掉了 2.3 handler 异常透传**；故这不算“报告谎报”，但属于第三方报告“结论：fallback 已修复” 的**部分性夸大**。准确表述应为：fallback 的**框架层**已修，但 **若干 handler 仍可吞异常**，完整 fallback 链路尚未生效。

### 保留 B 2 报告自称“18 项改造”，但按报告内部编号合计为 16 项

- P0-A (3) + P0-B (2) + P0-C (5) + P1-A (4) + P1-B (1) + P1-C (1) = **16 项**。
- 报告开头写“18 项改造全部落地”。若将 3.1 vision_reader 中的 lesson/homework 视作 2 项，则 17；仍不是 18。
- 这可能只是写错了一个数字，但会误导验收。建议修正为“16 项完成。”

### 3 报告已如实说明无法完成项 ✅

报告“九、未做的项”已列出：
- 完整 pytest 套件（未做）
- 真实 LLM 调用实测（未做）
- DAG 并行（推迟 V5.3）
- sqlite-vec（推迟 V5.3）
- CSRF/Origin（未做）

我实测 `backend/tests/` 目录不存在，与此一致。

---

## 四、另一处需注意（环境相关）

- 在 PowerShell 默认 cp1252 终端下直接 `python run.py` 会因 `run.py` 打印 Unicode 横幅报 `UnicodeEncodeError`。用 `python -X utf8 run.py` 可正常启动。
- 第三方报告未提到这一点。若用户经常在普通 cmd/PowerShell 双击启动，建议后续把 run.py 横幅改为 ASCII-only 或 `errors='replace'`，否则启动可能失败。

---

## 五、审查总结

| 维度 | 结论 |
|---|---|
| 改动真实性 | 16/16 分支逐项证据匹配（含仍然保留问题在内） |
| 是否有谎报 | 实质没有；但“18 项”与编号合计 16 不符，请复核 |
| 重要功能未完全落成 | ✅ 大部分落成；**fallback 链路仍受“ handler 吞异常”影响**，需继续 |
| 无法完成项说明 | 报告中“未做”清单与源码现状一致（无 backend/tests）；符合自查 |
| 前端/后端构建 | 前端 vite 构建一致通过；后端未中断启动 |

**验收建议：**
1. 把报告标题中的“18 项”改为“16 项”或补充说明口径。
2. 优先修 `dag_homework.py`、`dag_lesson.py` 中 5 处若网关失败应抛 `GatewayRetryableError` 而不是返回占位/空。
3. run.py 的 Unicode 横幅问题可顺手修为 ASCII / errors=replace。
